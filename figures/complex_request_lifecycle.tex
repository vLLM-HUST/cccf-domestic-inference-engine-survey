\begin{tikzpicture}[
    node distance=9mm and 8mm,
    stage/.style={draw=black!75, line width=0.8pt, rounded corners=2.2pt, minimum width=23mm, minimum height=10.5mm, align=center, font=\small, inner sep=4pt},
    side/.style={draw=black!70, line width=0.8pt, rounded corners=2.2pt, align=center, font=\scriptsize, inner sep=3pt},
    note/.style={font=\footnotesize, align=center, fill=white, inner sep=1.5pt, text=black!80},
    flow/.style={-{Latex[length=2.2mm]}, semithick, draw=black!75}
]
\node[stage, fill=gray!8] (ingress) {请求进入\\文本/多模态/工具调用};
\node[stage, fill=blue!9, right=of ingress] (normalize) {请求整形\\长度 bucket / SLO};
\node[stage, fill=green!10, right=of normalize] (prefill) {前处理与 Prefill\\视觉/语音编码};
\node[stage, fill=green!10, right=of prefill] (decode) {Decode\\采样/约束执行};
\node[stage, fill=orange!12, below=12mm of decode] (verify) {验证与回退\\grammar / speculative};
\node[stage, fill=gray!8, right=of verify] (finish) {响应返回\\状态释放与观测};

\node[side, fill=green!6, below=15mm of prefill] (cache) {共享状态\\KV Cache / prefix / 迁移元数据};
\node[side, fill=orange!7, below=15mm of finish] (tool) {外部交互\\工具执行 / 回注 / 再进入};

\draw[flow] (ingress) -- (normalize);
\draw[flow] (normalize) -- (prefill);
\draw[flow] (prefill) -- (decode);
\draw[flow] (decode) -- (verify);
\draw[flow] (verify) -- (finish);
\draw[flow, dashed] (prefill) -- (cache);
\draw[flow, dashed] (decode.south west) |- (cache.east);
\draw[flow, dashed] (verify.south east) |- (tool.west);
\draw[flow, dashed] (decode.south east) |- ([xshift=-3mm]tool.north);
\draw[flow, dashed] (tool.west) -| ([yshift=-1mm]normalize.south);

\node[note, text width=5.8cm, below=10mm of tool, xshift=8mm] {难点不在单步算子，而在跨阶段状态传递、资源复用与回退一致性。};
\end{tikzpicture}