\definecolor{cBlue}{HTML}{E6F0FA}
\definecolor{cGreen}{HTML}{E9F5EE}
\definecolor{cOrange}{HTML}{FCEFE3}
\definecolor{cGray}{HTML}{F2F3F5}
\definecolor{cSideA}{HTML}{E9EEF5}
\definecolor{cSideB}{HTML}{F5EFE6}
\begin{tikzpicture}[
    node distance=7mm and 7mm,
    scale=1.08,
    transform shape,
    stage/.style={draw=black!70, line width=0.85pt, rounded corners=2.4pt, minimum width=24mm, minimum height=11mm, align=center, font=\normalsize\sffamily, inner sep=4pt},
    side/.style={draw=black!65, line width=0.8pt, rounded corners=2.2pt, minimum width=28mm, align=center, font=\scriptsize\sffamily, inner sep=3pt},
    note/.style={font=\small\sffamily, align=center, fill=white, inner sep=1.6pt, text=black!75},
    flow/.style={-{Latex[length=3.0mm,width=2.0mm]}, line width=0.9pt, draw=black!75}
]
\node[stage, fill=cGray] (ingress) {\textbf{请求进入}\\文本/多模态/工具调用};
\node[stage, fill=cBlue, right=of ingress] (normalize) {\textbf{请求整形}\\长度 bucket / SLO};
\node[stage, fill=cGreen, below=12mm of normalize] (prefill) {\textbf{前处理与 Prefill}\\视觉/语音编码};
\node[stage, fill=cGreen, right=of prefill] (decode) {\textbf{Decode}\\采样/约束执行};
\node[stage, fill=cOrange, below=12mm of decode] (verify) {\textbf{验证与回退}\\grammar / speculative};
\node[stage, fill=cGray, right=of verify] (finish) {\textbf{响应返回}\\状态释放与观测};

\node[side, fill=cSideA, left=10mm of prefill] (cache) {\textbf{共享状态}\\KV Cache / prefix / 迁移元数据};
\node[side, fill=cSideB, right=10mm of decode] (tool) {\textbf{外部交互}\\工具执行 / 回注 / 再进入};

\draw[flow] (ingress) -- (normalize);
\draw[flow] (normalize) -- (prefill);
\draw[flow] (prefill) -- (decode);
\draw[flow] (decode) -- (verify);
\draw[flow] (verify) -- (finish);

\node[note, text width=8.6cm, below=10mm of verify, xshift=20mm] {复杂请求的关键难点不在单步执行，而在跨阶段状态传递、资源复用与回退一致性。};
\end{tikzpicture}