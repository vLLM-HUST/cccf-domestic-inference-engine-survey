\begin{tikzpicture}[
    node distance=9mm and 10mm,
    block/.style={draw=black!75, line width=0.8pt, rounded corners=2.2pt, align=center, minimum height=10mm, inner sep=4pt, font=\small},
    group/.style={draw=black!60, line width=0.8pt, rounded corners=3pt, inner sep=7pt},
    note/.style={font=\footnotesize, align=center, fill=white, inner sep=1.5pt, text=black!80},
    flow/.style={-{Latex[length=2.2mm]}, semithick, draw=black!75}
]
\node[block, fill=gray!8, minimum width=3.0cm] (ingress) {业务请求\\文本/长上下文/多模态};
\node[block, fill=blue!9, minimum width=2.9cm, right=of ingress] (service) {服务入口与接口层\\鉴权/OpenAI 兼容/监控};
\node[block, fill=blue!9, minimum width=3.0cm, right=of service] (scheduler) {调度与运行时层\\batch 组织/SLO/回退};
\node[block, fill=green!10, minimum width=2.55cm, below left=11mm and 7mm of scheduler] (execute) {执行层\\prefill/decode/并行};
\node[block, fill=green!10, minimum width=2.6cm, below=11mm of scheduler] (cache) {KV Cache 与状态\\前缀复用/迁移/驱逐};
\node[block, fill=green!10, minimum width=2.6cm, below right=11mm and 7mm of scheduler] (backend) {后端接口层\\图模式/算子/能力门控};
\node[group, fit=(execute)(cache)(backend), label={[font=\small, fill=white, inner sep=1.5pt]above:运行时核心}] (runtime) {};
\node[block, fill=orange!12, minimum width=2.8cm, right=15mm of backend] (compiler) {编译器与运行时\\CANN/MindSpore/插件栈};
\node[block, fill=orange!12, minimum width=2.55cm, below=9mm of compiler] (device) {国产硬件平台\\NPU/GPU/驱动/通信};

\draw[flow] (ingress) -- (service);
\draw[flow] (service) -- (scheduler);
\draw[flow] (scheduler) -- (execute);
\draw[flow] (scheduler) -- (cache);
\draw[flow] (scheduler) -- (backend);
\draw[flow] (execute) -- (cache);
\draw[flow] (cache) -- (backend);
\draw[flow] (backend) -- (compiler);
\draw[flow] (compiler) -- (device);
\draw[flow] (device.west) |- (backend.east);
\draw[flow, dashed] (cache.south) -- ++(0,-5mm) -| ([yshift=-2mm]service.south);

\node[note, text width=3.2cm, below=10mm of execute] {多阶段请求会迅速放大负载异质性};
\node[note, text width=3.6cm, below=10mm of device] {平台差异主要暴露在编译、后端与回退边界};
\end{tikzpicture}