% Purpose: redraw the original system-design logic with clearer spacing and routing.
\begin{tikzpicture}[
    font=\small,
    node distance=12mm and 8mm,
    block/.style={draw=black!78, line width=0.8pt, rounded corners=2pt,
        align=center, minimum height=10mm, inner sep=3pt, text width=30mm},
    core/.style={block, fill=green!8},
    platform/.style={block, fill=orange!10},
    note/.style={draw=black!45, line width=0.55pt, rounded corners=2pt,
        align=center, font=\footnotesize, inner sep=3pt, fill=gray!5, text width=42mm},
    tag/.style={font=\footnotesize\bfseries, text=black!75, align=center},
    flow/.style={-{Stealth[length=2.6mm]}, line width=0.95pt, draw=black!82,
        shorten >=2pt, shorten <=2pt},
    stateflow/.style={-{Stealth[length=2.4mm]}, line width=0.9pt, draw=black!70,
        dash pattern=on 3pt off 2pt, shorten >=2pt, shorten <=2pt}
]

\node[block, fill=gray!8] (ingress) {业务请求\\文本/长上下文/多模态};
\node[block, fill=blue!8, right=of ingress] (service) {服务入口与接口层\\鉴权 / OpenAI 兼容 / 监控};
\node[block, fill=blue!8, right=of service] (scheduler) {调度与运行时层\\batch 组织 / SLO / 回退};
\node[tag, above=4mm of service] {服务入口};

\node[core, below=16mm of service] (execute) {执行层\\prefill / decode / 并行};
\node[core, right=of execute] (cache) {KV Cache 与状态\\前缀复用 / 迁移 / 驱逐};
\node[core, right=of cache] (backend) {后端接口层\\图模式 / 算子 / 能力门控};
\node[tag, above=4mm of cache] {运行时核心};

\node[platform, right=13mm of backend] (compiler) {编译器与运行时\\CANN / MindSpore / 插件栈};
\node[platform, below=11mm of compiler] (device) {国产硬件平台\\NPU / GPU / 驱动 / 通信};
\node[tag, above=4mm of compiler] {平台边界};

\node[note, below=10mm of execute] (note1) {多阶段请求会迅速放大负载异质性};
\node[note, below=10mm of device] (note2) {平台差异主要暴露在编译、后端与回退边界};

\draw[flow] (ingress) -- (service);
\draw[flow] (service) -- (scheduler);

\draw[flow] (scheduler.south) -- ++(0,-7mm) -| (execute.north);
\draw[flow] (scheduler.south) -- (cache.north);
\draw[flow] (scheduler.south) -- ++(0,-7mm) -| (backend.north);

\draw[flow] (execute) -- (cache);
\draw[flow] (cache) -- (backend);
\draw[flow] (backend) -- (compiler);
\draw[flow] (compiler) -- (device);
\draw[flow] (device.west) -- ++(-9mm,0) |- (backend.south);

\draw[stateflow] (cache.north west) -- ++(-7mm,7mm) -| (service.south);

\end{tikzpicture}
